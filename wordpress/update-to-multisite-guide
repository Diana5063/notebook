#将wordpress单站点变更为multisite

1.切换不需要woocommerce插件(最好不需要任何插件)的主题


2.关闭所有插件
根据依赖顺序，先关闭不被依赖的插件，被依赖的最后关闭


3.开启multisite
3.1 在wp-config.php中加入 define('WP_ALLOW_MULTISITE', true);

3.2 刷新后台，点击Tools->Network Setup
(因为不是全新multisite站点，所以创建子站点只能通过subdomain的方式)
Create a Network of WordPress Sites，填写Network Title和Network Admin Email，点击Install

3.3 将以下配置添加到config.php
define( 'MULTISITE', true );
define( 'SUBDOMAIN_INSTALL', true );
define( 'DOMAIN_CURRENT_SITE', 'xxx.net' );
define( 'PATH_CURRENT_SITE', '/' );
define( 'SITE_ID_CURRENT_SITE', 1 );
define( 'BLOG_ID_CURRENT_SITE', 1 );

3.4 配置nginx伪静态
参考官方指导：https://developer.wordpress.org/advanced-administration/server/web-server/nginx/
在phpstudy-网站，选择网站，点击管理-修改-伪静态，填入以下内容

if (!-e $request_filename) {
   rewrite /wp-admin$ $scheme://$host$request_uri/ permanent;
   rewrite ^(/[^/]+)?(/wp-.*) $2 last;
   rewrite ^(/[^/]+)?(/.*\.php) $2 last;
}

location / {
   try_files $uri $uri/ /index.php?$args ;
}


4.配置DNS(生产必需)
如果用子域名：
在 DNS 添加：
A记录：
*.example.com → 你的服务器IP


5.重新登录后台
此时进入的是Multisite管理后台，
5.1 在Multisite管理后台->Themes，将所有主题开启Network Enabled，开启后子站点才能使用这些主题
5.2 在Multisite管理后台->Plugins，可看到原站点已安装的插件，此时插件全部是Network Deactivate，此时所有子站点都可以看到所有插件
    将Redis Object Cache插件开启Network Activate，其他插件保持Network Deactivate即可
    安装迁移插件NS Cloner – Site Copier，开启Network Activate


6.恢复主站数据
在Multisite管理后台->Sites列表，主站那一行，从Dashboard进入主站后台，

6.1切回原主题
点击Appearance->Themes,开启原站使用的主题及子主题，开启主题时会弹出设置向导，跳过此步骤即可

6.2开启插件
点击Plugins进入插件列表，开启所有原来需要的插件，按依赖顺序，被依赖的先开启


7.克隆主站到子站
点击左上角-My Sites->Network Admin->NS Cloner，进入克隆插件面板，填写克隆信息：
Select Cloning Mode:Standard Clone
Select Source:ID为1的站点(主站)
Create New Site:填写克隆的子站标题、子域名等
其他选项都默认即可，点击右下角CLONE就开始克隆了，克隆完成后需要到新站点开启需要的插件和激活主题
注意：避免插件冲突，开启插件时不要全选开启所有，先开启被依赖的主插件(如 Elementor,Woocommerce等)，再分批小批量开启其他非自研插件，最后开启自研插件


8.访问子站
使用子站域名访问前台页面，可以看到跟主站完全相同的子站


9.清理主站
待子站运行一段时间没问题后，可以逐步清理主站的数据和关闭不需要的插件、更换主题等