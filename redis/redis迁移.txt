迁移步骤：
1、先写get, set的公用函数, 所有项目原先的调用代码都走这些函数；
2、双写数据，在上面的set 函数里同时set 新老redis(数据库如果有update, del 也一样操作)；
3、同步数据，写脚本把老的redis数据(或者扫一遍DB)补充到新的redis 里，保证新老数据库完全一致；
4、把读切到新redis库，观察一段时间没问题了，停止老库写入；
5、删除老redis库数据。