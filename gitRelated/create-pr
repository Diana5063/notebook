#创建一个ＰＲ并发送的步骤
首先要从目标仓库fork一个自己的仓库，记得是fork而不是克隆，不然无法发pr。
１、提交本地代码；
２、基于要发ＰＲ的仓库相应分支最新代码（比如目标远程分支是master）检出一个本地新分支。
切换到这个新分支，将之前提交的本地代码按提交顺序分别cherry-pick到当前分支。
将当前分支代码推送到自己远程仓库。
３、在远程仓库中找到刚才的推送。选择create merge requests，创建一个合并请求。
源分支是自己推送的最新分支，目标分支是对方的master分支，提交。
４、完成。
